apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab4-backend
  namespace: comp-math
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lab4-backend
  template:
    metadata:
      labels:
        app: lab4-backend
    spec:
      containers:
        - name: lab4-backend-container
          image: artem5252/cm-4-backend:latest
          ports:
            - containerPort: 8080