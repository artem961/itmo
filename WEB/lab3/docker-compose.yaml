version: '3.9'

services:
  postgres:
    image: postgres:17
    container_name: lab3_database
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: lab3
    ports:
      - "5432:5432"
    restart: unless-stopped